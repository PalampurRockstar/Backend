server:
  port: 3333

spring:
  application:
    name: configuration
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/PalampurRockstar/Backend'
          clone-on-start: true
          search-paths: 'common-configuration'

